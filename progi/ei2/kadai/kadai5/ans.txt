問題19
(1)
	これらの関数を使う人からすると、目的結果が同じでも、
	そのための手段は多い方が便利だから。

(2)
	ptGetX(),
	ptGetY(),
	ptCreate(),
	rctWidth(),
	rctHeight(),
	rctTopLeftCorner(),
	rctCreate1(),
	rctCreate2(),
	
(7)
    rctHeight(),
    rctWidth(),
    rctCreate1(),
    rctCreate2(),
	
(9)
    ptGetX(),
    ptGetY(),
	
(10)
	 データの外部仕様の設計では、
	データの内部仕様の実装方法とは分離し、
	実装方法が変わっても外部仕様は変わらないようにする事が重要。
	 データの内部仕様の実装では、
	あとから変更することが少なくなるようにし、
	もし変更することがあっても、その影響が外部仕様にまで及ばないようにすることが重要。
